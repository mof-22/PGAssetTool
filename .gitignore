bin/
workspace/
obj/
.vs/
.idea/
*.user


# Local index cache and mod working directories
*.db
*.db-shm
*.db-wal

# Command output dumped while exploring. These contain game-derived data.
*.txt
!requirements*.txt

# Backups and installed-mod state. Kept beside the tool so uninstalling the game cannot
# reach them, but they are local state and game-derived, never committed.
PGAssetTool-data/

# Avalonia's XAML compiler writes generated sources here
src/PGAssetTool.Gui/obj/
