# PGAssetTool

[English](README.md) / 日本語

<p align="center">
  <a href="https://github.com/mof-22/PGAssetTool/releases"><img src="https://img.shields.io/github/downloads/mof-22/PGAssetTool/total.svg?style=for-the-badge"></a>
  <a href="https://github.com/mof-22/PGAssetTool/releases"><img src="https://img.shields.io/github/v/release/mof-22/PGAssetTool?style=for-the-badge"></a>
</p>

某ピクセル調ゲームのアセット改変を簡単にするWindows用ツールです。
- アイテムが参照しているモデルやテクスチャなどをまとめて閲覧する**Browse**
- それらのデータを改変したり、MODパックとして書き出せる**Editor**
- ダウンロードしたMODパックの管理ができる**Manager**

など、アセットに関わる動作をとても簡単に行えます。

## **各種ゲーム内アイテムの対応状況**

| 種別      | 対応状況 | 詳細                 |
|---------|------|--------------------|
| 武器      | ✅️   |                    |
| 武器スキン   | ✅️   |                    |
| ハット     | ⚠️   | CLIでのみ。非推奨かつサポートなし |
| マスク     | ⚠️   | CLIでのみ。非推奨かつサポートなし |
| アバター    | ⚠️   | CLIでのみ。非推奨かつサポートなし |
| マント     | ⚠️   | CLIでのみ。非推奨かつサポートなし |
| ブーツ     | ⚠️   | CLIでのみ。非推奨かつサポートなし |
| グライダー   | ⚠️   | CLIでのみ。非推奨かつサポートなし |
| 移動      | ⚠️   | CLIでのみ。非推奨かつサポートなし |
| ペット     | ⚠️   | CLIでのみ。非推奨かつサポートなし |
| ガジェット   | ❌️   | 将来的に対応予定           |
| ボタン等UI  | ❌️   | 将来的に対応予定           |
| ハットスキン  | ❌️   | 将来的に対応予定           |
| マスクスキン  | ❌️   | 将来的に対応予定           |
| マントスキン  | ❌️   | 将来的に対応予定           |
| ブーツスキン  | ❌️   | 将来的に対応予定           |
| ロビーアイテム | ❌️   | 未定                 |
| アーマー    | ❌️   | 未定                 |
| 落書き     | ❌️   | 未定                 |

**その他の種別についても検討中/未定です。**

## 入手する

前提として、Windows x64である必要があります。

このツールはexeファイルと同じ階層に設定やバックアップを保持するため、**ダウンロード後はexeファイルを任意のフォルダに移動することをおすすめします。** 以下は一例です。\
`C:/Users/%USER%/downloads/PGAssetTool.exe` → `C:/Users/%USER%/Tools/PGAT/PGAssetTool.exe`

### Releasesからビルド済みツールをダウンロードする
右側にあるReleasesから最新のバージョンをダウンロードしてください。


### ソースからビルドする

自身でツールをビルドする場合、別途`.NET 10 SDK`が必要です。

```
dotnet publish src/PGAssetTool.Gui -c Release -o dist
```

---

## MODパックの作成方法

WIP...

---

## 3つのタブ

**Browse** - 武器の一覧と、選んだものが参照しているデータのツリーが表示されます。\
ゲーム内ギャラリー番号, 各言語の武器名、内部武器番号での検索に対応しています。\
データのツリーでは`Ctrl+Shift+R`のショートカットを使用することで簡易モードになります。

右側のタブではプレビューが表示されており、実際のモデルを確認したり関連スキンを表示できます。各アニメーションなども再生可能です。

**Editor** - 抽出したデータのワークスペースなどから各要素の比較などができます。
左下のPack detailsから該当のワークスペースの詳細を変更できます。\
一つ以上のデータを変更した状態でBuild packボタンを押すと、MODパックである`.pgmod`ファイルがワークスペースフォルダに作成されます。\
Build and applyを使用することで、MODパックをワンクリックでインストールできます。

**Manager** - 導入したMODパックを管理できます。\
MODのON/OFFが可能なTurn on/off\
ツール上から削除するRemove(`SHIFT`を押しながらでファイルごと削除できます。)\
有効なMODをすべて書き直すReapply all(ゲームの更新時などに有用)などがあります。\
また、`Ctrl` + ホイールで一覧表示の大きさを変更することもできます。\
左側のタブでBrowseと同じ検索を用いて当てはまるMODのみを一覧表示することも可能です。

---

## 第三者のMODパックのインストール

`.pgmod`ファイルをウィンドウにドラッグしてください。複数選択も対応しています。

---

## ショートカット

| キー | 動作 |
| --- | --- |
| `Ctrl+1` `Ctrl+2` `Ctrl+3` | Browse / Editor / Manager |
| `Ctrl+F` | そのタブの検索欄へ |
| `Ctrl+E` | 選択中の武器を抽出 |
| `Ctrl+Shift+E` | 選択中のアセットだけを抽出 |
| `Shift+A` | 今見えている画像のアルファを表示 |
| `Ctrl+Shift+R` | 差し替えられるものだけ表示 |
| `Ctrl+R` | ゲームをディスクから読み直す |
| `F5` | ワークスペースを読み直す |
| `Delete` | 選択中のワークスペースを削除／選択中のMODを取り除く |
| `Ctrl+,` | オプション |

モデルが表示されているとき：

| | |
| --- | --- |
| ドラッグ | 回す |
| `Shift` + ドラッグ | 傾ける |
| 中ボタン + ドラッグ | 枠の中で動かす |
| ホイール | 近づく・離れる |
| `R` | まっすぐに戻す |

---

## うまくいかないとき

**ゲームが見つからない** - Steam版を想定していますが、Epic版を使用しているなどの理由で問題起きる場合は、
**File → Options** から自分で指定してください。ゲームの `*_Data` フォルダを含んでいるフォルダです。

**バンドルが他のものに書き換えられている** - このツールが変更しようとしているデータがすでになんらかの方法で変更されています。steamの整合性チェックを用いてバニラのデータに戻してください。

**メモリを使いすぎる** - **File → Options** の2つの設定で、速さと引き換えに減らせます。*Memory for reading
the game faster* は閲覧したバンドルを展開して置いておく量で、既定は 1GB です。減らすと選択や抽出が
遅くなります。*Rebuild one bundle at a time* を有効にすると、導入や切り替えが約3割遅くなる代わりに
メモリが約3割減ります。

**PCの移行・再インストール時** - `PGAssetTool-data` フォルダの中身をまるごと移してください。
特に `author.key` はパックの署名に使う鍵で、失うと以降のパックが同じ作者のものだと示せなくなります。
安全な場所にコピーを残しておくことをおすすめします。

**入れたはずのMODが入れ替わる** - 1つのゲームに対して2つのツールから導入すると起こります。
[KNOWN_ISSUES.md](KNOWN_ISSUES.md) を参照してください。

**ツールが落ちた・不具合を見つけた** - `PGAssetTool-data/logs` の中にある `crash-日時.txt`
（落ちた場合）や `errors.log` を添えて報告してください。File → Open the log folder から開けます。

---

## コマンドライン

GUI でできることは全部あります。実行ファイルは
`src/PGAssetTool.Cli/bin/Release/net10.0-windows/win-x64/pgassettool.exe`、または：

```
dotnet run --project src/PGAssetTool.Cli -c Release -- <コマンド>
```

| コマンド | 動作 |
| --- | --- |
| `info` | 検出したインストールとバージョンを表示。 |
| `weapons [<絞り込み>]` | 武器一覧。名前・内部番号・タグ・ギャラリー番号で絞れます。 |
| `items [<種別>]` | 武器以外の種別 - 帽子・ケープ・マスク・ブーツ・ペット・グライダー・乗り物・アバター - の一覧、または指定した1種別の全件。 |
| `show <アイテム>` | 1つのアイテムと、それが参照しているものすべて。武器はゲーム内番号（`819`）、プレハブ名（`Weapon1257`）、スラッグのどれでも。それ以外は `items` が出す id です。**ゲーム内番号とプレハブ番号は別の連番です。** |
| `extract <アイテム>` | アイテムに属するものを全部書き出し。`--workspace` を付けるとパック化できる `pgmod.json` も書きます。 |
| `pack [<ディレクトリ>]` | ワークスペースから `.pgmod` をビルド。抽出以降に編集されたファイルだけが入ります。 |
| `apply <パック>` | `.pgmod` をゲームに導入。 |
| `mods` | 導入済み一覧。 |
| `enable <id>` / `disable <id>` | MODを有効化／アンインストールせずに無効化。 |
| `remove <id>` | MODをアンインストール。 |
| `verify` | 全バンドルを、ゲームが記録したハッシュと照合。 |
| `consolidate` | ダウンロード済みキャッシュを空にすると何が変わるかを報告。`--apply` は何も変わらないコピーだけを削除。 |

| オプション | 動作 |
| --- | --- |
| `--game <ディレクトリ>` | 検出したものではなくこのインストールを使う。想定どおりの構成なら何でもよいので、ライブのゲームではなくコピーに対して作業できます。 |
| `--language <バンドル>` | 名前を読む翻訳バンドル（既定 `l_en-gb`）。 |
| `--out <ディレクトリ>` | 出力先。`extract` の既定は `./workspace`。 |
| `--author <名前>` | マニフェストに記録されます。 |
| `--skin <id か 名前>` | 素の武器ではなく、指定したスキンを書き出す。何があるかは `show` が出します。 |
| `--opaque` | アルファチャンネル無しでテクスチャを書き出す。一部の武器は透明度を扱うため、画像編集アプリがアルファ値の編集に長けていない場合、この機能を用いてみてください。 |
| `--whole` | テクスチャを丸ごと書き出す。既定では**どのモデルも使わない部分を透明**にして、実際に見える範囲だけを残します。 |
| `--protect` | ビルドしたパックに署名し、zip として開けないようにする。 |
| `--force` | `apply` をそのまま進める。既に改変済みのバンドルでもバックアップを取り、署名済みパックがビルド後に改変されていても導入します。 |
| `--fast` | 書き戻すバンドルの圧縮を軽くする。その工程が約4倍速くなり、バンドルは約1.2倍になります。ゲーム側の読み込み速度は変わりません。 |
| `--rebuild` | 既に正しい内容が入っているバンドルも含めて全部作り直す。通常はそのまま残します。 |
| `--low-memory` | バンドルを4つ同時ではなく1つずつ作り直す。約3割遅くなり、メモリのピークは約3割減ります。 |
| `--read-memory <MB>` | `show` と `extract` がゲームのデータを展開してメモリに置いてよい量（既定 1024）。読み込みが数倍速くなります。`0` にすると全部ディスクから読みます。 |

```
pgassettool weapons crystal
pgassettool extract 819 --workspace --out workspace
pgassettool pack workspace/0819_something
pgassettool apply workspace/0819_something/something.pgmod
```

---

## 何を変えられるか

現在は以下のデータに対応しています。
| 対象 | 元にするファイル |
| --- | --- |
| テクスチャ | `.png` |
| モデル | `.glb` |
| 音声 | `.wav` `.mp3` `.ogg` |

すべて一般的な編集ソフトで開くことができます。

---

## ツールを改造する人へ

[DESIGN.md](DESIGN.md)（英語）に、なぜこの作りなのかが書いてあります - アイテムの部品をどうやって
見つけているか、パックとは何か、ゲームの更新にどう追従するか、そして**一見おかしいが理由があって
そうなっている箇所**。

```
dotnet test -c Release
dotnet run --project src/PGAssetTool.Gui -c Release -- --self-test
```

`dotnet test` はゲーム不要で、CI が回すものです。`--self-test` が本当の検査で、
実際のインストールに対してツール全体を動かし、終わったら元に戻します。

`classdata.tpk`（Unity のエンジンクラスデータベース）はコアアセンブリに埋め込まれています。
`*_Data` 以下のファイルは TypeTree 無しでビルドされているため必要です（AssetBundle は自前で持っています）。
**Unity 標準のクラスを記述したものだけで、ゲーム独自のコードとは無関係です。**
[THIRD-PARTY-NOTICES.md](THIRD-PARTY-NOTICES.md) を参照してください。

---

## 注意事項

- このツールは非公式のもので、ゲームの開発・運営元とは一切関係ありません。
- ゲームのファイルを書き換えるため、利用は自己責任でお願いします。アカウントへの影響（BAN等）を含め、
  このツールの使用によって生じたいかなる損害についても作者は責任を負いません。
- 他人の作品を無断で再配布しないでください。

---

## ライセンス

MIT。[LICENSE](LICENSE) を参照。

このプロジェクトはゲームのデータを一切同梱していません。MODパックは変更内容を宣言的に記述したもので、
参照しているのは利用者自身のインストールにあるアセットです。
